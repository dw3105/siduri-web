---
name: siduri-pending-tasks
description: "Eighteen rules awaiting centralisation; delete this file when the shared skills exist."
---

The shared skills being built in `dw3105/agent-skills` delete this file when they exist. This file holds eighteen rules awaiting centralisation.

## codex-tasks

- **SS-04** **Never end turn with uncommitted work in tree.** `git status --short` is one call. Backfill of ten finding row and seven decision row sat uncommitted six tool call and died to `git reset --hard` run beside it. [pending-centralisation]
- **FO-10** **Wave get one Done-when that is end-to-end and performed by person.** Read all lane Done-when and ask which name product. All naming file mean nobody test composition. [pending-centralisation]
- **PR-13** **Field carrying command line get re-run like `ran`, or carry prose and no command.** De-quoting address to pass secret scanner turned runnable `grep` into one matching nothing, while sentence around it still claim it showed both location. Survived because harness validate field presence, never `notes`. [pending-centralisation]
- **SH-07** **Commit message two `-m` flag.** Subject under 72 char, body max six line. [pending-centralisation]
- **SH-09** **Command shipped only after running here, or after reading its `--help` on this binary.** [pending-centralisation]
- **CL-01** **Done-when is list cited from task, and every line met or deferred by name.** Answered from memory is not answered. [pending-centralisation]

## shared-host

- **FO-05** **Value unique across lane is allocated in task, never chosen by worker.** Counter claimed on unmerged branch is not claimed. [pending-centralisation]

## writing-skills

- **ST-02** Caveman lite is full sentence, article, technical term, code, command, error string, filename all survive. [pending-centralisation]
- **ST-03** Scan before send. Cut opener, filler (just, really, basically, actually, simply), hedge, closer, tool narration. **One filler = scan not run.** [pending-centralisation]
- **ST-04** **Every reference carry its content same sentence, every turn.** `FO-04` is nothing; *FO-04, registration happen when data exist* is readable. [pending-centralisation]

## proposed for proving

- **PR-14** **Guard pinned to value it guard go red on correct work. Derive target twice by different mean, compare, never name it.** Tool built to remove pinned line range pinned count `18` in own selftest - same coupling one layer up. [pending-centralisation]

## proposed for evidence-and-claims

- **ST-05** **Fact leaving session carry where it was read from.** Ref for file, command for query, unit for number. [pending-centralisation]
- **RD-02** **Probe this turn beat every document.** Undated machine claim is hypothesis. Measured: friction row three day old name a sysctl as cause; sysctl read `0` and shape exit clean. Document promoted to machine fact is how it happen. [pending-centralisation]
- **RD-03** **Never restate machine fact or code fact from memory.** Three sibling repo carry three different git identity; one recalled cover all three is wrong twice. [pending-centralisation]

## proposed for talking-to-operator

- **SS-01** **Session is three phase: close carry-in, do work, deposit record.** Third phase is where every loss measured here happened. It is not closing paragraph, and it is not optional because turn ran long. [pending-centralisation]
- **SS-07** **Never assert where session sit in own context.** Harness summarise when conversation grow long. Guess wearing confidence of measurement justify stopping, and operator cannot check it. [pending-centralisation]
- **CL-03** **Claim later action erase carry its date and that action.** [pending-centralisation]

## proposed for reviewing

- **RV-03** **Reviewer claim is claim. Re-derive load-bearing half, say which half.** [pending-centralisation]

## skill check

Eighteen shared rules remain pending centralisation.
