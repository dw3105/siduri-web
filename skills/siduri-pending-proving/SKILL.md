---
name: siduri-pending-proving
description: "Temporary holding file for rules awaiting centralisation; it will be deleted when the shared skills exist."
---

This temporary holding file is deleted when the shared skills being built in `dw3105/agent-skills` exist. Under the operator's ruling, these centralised rules go into NEW shared skills rather than the existing eight.

## proposed for proving

- **FO-19** **Where correctness is per-part, defect live in relation between part, and no gate examining one part see it.** Operator found two site chrome, three post-meta, two content width in five minute; each copy internally consistent and green alone. `hitl` met same shape three time same day without rendering involved - guard inert exactly while it mattered, probe attached to claim it did not measure, one link parser silently dropping anchor shape and blinding seven guard each individually correct. Rendering is one instance, never the class. This is *why* `FO-10a`, not restatement of it. [pending-centralisation]
- **PR-02** **Prefer check that delete workaround over check that stand beside it.** `grep -c ArticlePage build.go` returning `0` is red today and green only when mechanism real. [pending-centralisation]
- **PR-05** **Proof land as committed check, never as demonstration then deleted.** Deleted demonstration bind memory. [pending-centralisation]
- **PR-07** **Determinism proved by recursive checksum over tree, never by eye.** [pending-centralisation]
- **PR-08** **Defect invisible to single run need test that run twice.** Package-global state survive build; one build see nothing. [pending-centralisation]
- **PR-12** **Check on check need same standard as check.** Grep for `AKIA` where tool print `AWS access key` report working scan broken. Plant landing after frontmatter delimiter report working guard absent. Name input, read actual output, confirm plant landed where aimed. [pending-centralisation]
- **PR-14** **Guard pinned to value it guard go red on correct work. Derive target twice by different mean, compare, never name it.** Tool built to remove pinned line range pinned count `18` in own selftest - same coupling one layer up. [pending-centralisation]
- **PR-15** **Directory doing job of exempted directory need its exemption, or it is record that cannot record.** `acceptance/` sit outside `docs/` so no lane edit contract; secret scanner exempt by prefix `docs/`, so fragment quoting the address it found turned `make check` red on own evidence. Scope follow role, never path. [pending-centralisation]

## proposed for evidence-and-claims

- **ST-05** **Fact leaving session carry where it was read from.** Ref for file, command for query, unit for number. [pending-centralisation]
- **RD-02** **Probe this turn beat every document.** Undated machine claim is hypothesis. Measured: friction row three day old name a sysctl as cause; sysctl read `0` and shape exit clean. Document promoted to machine fact is how it happen. [pending-centralisation]
- **RD-03** **Never restate machine fact or code fact from memory.** Three sibling repo carry three different git identity; one recalled cover all three is wrong twice. [pending-centralisation]
- **PR-16** **Report grouping row under one probe get two property checked, never one: block verdict equal table verdict of every row it name, and block partition row set exactly once.** Grouping several annotation of one defect under one probe is right, and it is also where verdict hide. Both property are mechanical and found real contradiction by hand in a minute - one annotation carrying three verdict across two report, each defensible as of different date, none saying which was current. [pending-centralisation]

## proposed for talking-to-operator

- **SS-01** **Session is three phase: close carry-in, do work, deposit record.** Third phase is where every loss measured here happened. It is not closing paragraph, and it is not optional because turn ran long. [pending-centralisation]
- **SS-07** **Never assert where session sit in own context.** Harness summarise when conversation grow long. Guess wearing confidence of measurement justify stopping, and operator cannot check it. [pending-centralisation]
- **AS-08** **Question that contradict operator's own instruction go to him as contradiction, named.** Contract he wrote can forbid order he asked for. [pending-centralisation]
- **CL-03** **Claim later action erase carry its date and that action.** [pending-centralisation]

## proposed for reviewing

- **RV-02** **Agreement carry scope: what reviewer checked, what they did not.** Agreement with no scope become laundering three week later. [pending-centralisation]
- **RV-03** **Reviewer claim is claim. Re-derive load-bearing half, say which half.** [pending-centralisation]

## skill check

Delete this file once every proposed shared skill exists.
