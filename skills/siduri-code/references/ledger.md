# ledger - rule provenance

**v1.0 - 25 Aug 2026.**

## v1.0 - 25 Aug 2026

Derived from `gateslot-code` v1.13. Structure kept. Content earned in one day:
repo genesis, one spine lane, one seam lane, eleven findings.

### Dropped, and why

| Rule family | Why it does not apply |
|---|---|
| `EN-*` environment | One machine, no privileged surface, no second host this session cannot see. |
| `SS-*` session shape | Work here is waves of parallel worker, not one step per session. `FO-*` replace it. |
| `PE-*` people | No client, no board. |
| Contract-test naming | No requirement-id-to-test convention yet; `CT-06` hold the property without prescribing a name. |

### Earned this day

| Rule | Earned by |
|---|---|
| `FO-01` | Codex worker read task file and nothing else; every clause meant to bind one must be inlined or become a guard. |
| `FO-02` | `project_doc_max_bytes = 32768` read out of the binary, unset in config, silent, drops the end - where the house form puts enforcement. |
| `FO-04` | Every `Register` call sit in `init()` while content load inside `Build()`; four of eight lane cannot register at all. |
| `FO-12` | Writing all eight lane task before cutting any worktree surfaced four shared line invisible from inside a lane. |
| `PR-01` | The disjointness proof was green against the un-widened seam, because a worked example already in the tree satisfied it. |
| `PR-02` | `grep -c ArticlePage build.go → 0` is red at `1` today and cannot be satisfied by a demo standing beside the defect. |
| `PR-03` | `git diff --name-only` report zero untracked file and report the tracked generated file it never asked about. |
| `PR-08` | Package-global route set panic on the second build in one process, and nothing in the tree call build twice. |
| `RD-02` | A friction row three day old was promoted to a current machine fact; the sysctl it named read `0`. |
| `RD-03` | Three sibling repo carry three different git identity; one recalled cover all three was wrong twice. |
| `CT-05` | Renaming both spec on the way in dangled four reference inside a document only the operator may edit. |
| `SH-03` | Worktree path resolve against `-C`; a bare name nest inside the repo where every gate walk it. |
| `SH-04` | No `~/.gitconfig`; unset identity kill every worker at its first commit, after all the work. |
| `SH-05` | Fresh `git init` here give `master`. |
| `RV-02` | Agreement recorded without scope is what become laundering later; the reviewer asked for this against their own agreement. |

### Rule whose author broke it the same day

`RD-02` and `RD-03`. Both written into the plan by a session that had, within the
hour, applied them to a peer's claim and not to its own. `PR-01` likewise: shipped
as the certificate the entire eight-lane cut rested on, green at baseline.

This is the ordinary shape rather than the exception. Clause that look obvious is
clause somebody found obvious and missed anyway.
